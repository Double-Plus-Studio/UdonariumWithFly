import { ResizableDirective } from './resizable.directive';

describe('ResizableDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableDirective(null as any, null as any);
    expect(directive).toBeTruthy();
  });
});
