import { TooltipDirective } from './tooltip.directive';

describe('TooltipDirective', () => {
  xit('should create an instance', () => {
    const directive = new TooltipDirective(null as any, null as any, null as any);
    expect(directive).toBeTruthy();
  });
});
